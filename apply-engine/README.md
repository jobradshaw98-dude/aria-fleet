# apply-engine → moved

This automation engine now lives in its own installable repository:

## → [github.com/jobradshaw98-dude/career-engine](https://github.com/jobradshaw98-dude/career-engine)

It was expanded into the full job-search pipeline — **source → qualify → build → apply → engage** —
with a standalone `pip install`, a `career-engine` CLI, a generic fictional sample applicant, and its
own test suite + CI. (It still never submits on its own.)

This folder is kept only as a pointer so older links don't 404.
